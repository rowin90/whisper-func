{
  "mcpServers": {
    "whisper-func": {
      "command": "pnpm",
      "args": ["run", "mcp:server"],
      "env": {
        "OPENAI_API_KEY": "${OPENAI_API_KEY}",
        "OPENAI_BASE_URL": "${OPENAI_BASE_URL}",
        "OPENAI_MODEL": "${OPENAI_MODEL}"
      }
    }
  }
}
